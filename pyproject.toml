[project]
name = "ai-course-factory"
version = "0.1.0"
description = "AI Course Factory MVP vertical slice"
requires-python = ">=3.12,<3.13"
dependencies = [
    "langgraph>=1,<2",
    "langgraph-checkpoint-sqlite==3.1.1",
]

[tool.uv]
package = false
